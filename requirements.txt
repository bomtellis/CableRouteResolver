# Cable Routing Solver runtime dependencies
# Verified against all Python imports in app.zip on 2026-07-01.
# Requires Python 3.9 or newer.
# Install with: python -m pip install -r requirements.txt

PySide6>=6.7,<7
ezdxf>=1.3,<2
PuLP>=2.8,<4
